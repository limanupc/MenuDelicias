<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :pro_nombre %><br>
    <%= f.text_field :pro_nombre %>
  </div>
  <div class="field">
    <%= f.label :pro_comentario %><br>
    <%= f.text_field :pro_comentario %>
  </div>
  <div class="field">
    <%= f.label :pro_precio %><br>
    <%= f.text_field :pro_precio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
