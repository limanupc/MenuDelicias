<%= form_for(@identuty_type) do |f| %>
  <% if @identuty_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@identuty_type.errors.count, "error") %> prohibited this identuty_type from being saved:</h2>

      <ul>
      <% @identuty_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
