<%= form_for(@sex) do |f| %>
  <% if @sex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sex.errors.count, "error") %> prohibited this sex from being saved:</h2>

      <ul>
      <% @sex.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
