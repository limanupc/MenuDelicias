<h1>Listing sexes</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sexes.each do |sex| %>
      <tr>
        <td><%= sex.description %></td>
        <td><%= link_to 'Show', sex %></td>
        <td><%= link_to 'Edit', edit_sex_path(sex) %></td>
        <td><%= link_to 'Destroy', sex, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sex', new_sex_path %>
